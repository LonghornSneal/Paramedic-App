/* A simple JavaScript source file (utility function) used primarily for testing or demonstration. For example, it might export a basic function (like an addition function) which is tested by add.test.js. This file isn’t part of the core app features; instead, it’s used to ensure the development and testing pipeline (like unit tests) is set up correctly.
*/
function add(a, b) {
  return a + b;
}
module.exports = add;
